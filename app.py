import streamlit as st

from logic import common as c
from ui_helpers import require_password, sidebar_data_source

st.set_page_config(page_title="ГЭК: приказы, протоколы, отчёты", page_icon="🎓", layout="wide")
require_password()

sid = sidebar_data_source()

st.title("🎓 Документы ГЭК — генератор")
st.caption("Все PDF собираются из Google-таблицы «ВКР UU» и стилизованы под шаблоны АНО ВО «Универсальный университет».")

st.markdown(f"""
Данные читаются напрямую из **[Google-таблицы](  {c.sheet_url(sid)}  )** (лист «{c.SHEET_STUDENTS}» —
обучающиеся, «{c.SHEET_GEK}» — состав комиссий, «{c.SHEET_SCHEDULE}» — даты защит).
Открой её по ссылке слева, заполни нужные строки — и возвращайся сюда генерировать документы.
""")

st.divider()

st.page_link("pages/0_Сгенерировать_всё.py", label="🗂️ Сгенерировать всё разом (приказы + протоколы + заявления + …)", icon="🗂️")

st.divider()

st.subheader("📑 Приказы")
col1, col2 = st.columns(2)
with col1:
    st.page_link("pages/1_Приказ_темы_и_руководители.py", label="Темы и руководители ВКР", icon="📝")
    st.page_link("pages/2_Приказ_допуск_к_ГИА.py", label="Допуск к ГИА", icon="✅")
    st.page_link("pages/3_Приказ_состав_ГЭК.py", label="Состав и секретари ГЭК", icon="👥")
with col2:
    st.page_link("pages/4_Приказ_расписание_ГИА.py", label="Расписание ГИА", icon="🗓️")
    st.page_link("pages/5_Приказ_апелляционная_комиссия.py", label="Апелляционная комиссия", icon="⚖️")

st.subheader("📄 Протоколы, отчёты и отзывы ГЭК")
col3, col4 = st.columns(2)
with col3:
    st.page_link("pages/6_Протоколы_ГЭК.py", label="Протоколы заседаний ГЭК", icon="📄")
    st.page_link("pages/7_Отчёт_председателя_ГЭК.py", label="Отчёт председателя ГЭК", icon="📊")
with col4:
    st.page_link("pages/8_Отзывы_и_рецензии.py", label="Отзывы и рецензии на ВКР", icon="✍️")
    st.page_link("pages/9_Заявления_студентов.py", label="Заявления студентов", icon="🖊️")

st.divider()

with st.expander("ℹ️ Как это устроено"):
    st.markdown("""
- Каждая страница слева — отдельный генератор. Заполни реквизиты (номер/дату приказа и т.п.),
  нажми **«Сгенерировать»** — получишь PDF по каждому направлению/студенту плюс общий `.zip`.
- **Всё сразу vs выборочно**: у каждого генератора (кроме апелляционной комиссии — она одна на
  всех) есть блок **«🎯 Что генерировать — всё или выборочно»** с фильтром по факультету /
  направлению / программе. Ничего не выбрано = генерируется для всех. А страница
  **«🗂️ Сгенерировать всё разом»** прогоняет все типы документов за один клик и с одним фильтром,
  и отдаёт один общий `.zip` с подпапками по типам документов.
- Источник данных общий для всех страниц — ID таблицы задаётся в сайдбаре один раз.
- **Заявления студентов** (тема ВКР / ознакомление с ГИА / последипломные каникулы) —
  адресованы М.В. Марковой, один PDF на каждого обучающегося.
- **Отзывы и рецензии** — особый случай: сами тексты заполняются рецензентами в отдельных
  Google-таблицах, которые создаёт скрипт Apps Script `generate_review_tables.gs`
  (кнопка «ВКР» в меню самой таблицы). Здесь только собираются финальные PDF — подробности
  на странице «Отзывы и рецензии».
- Все документы — PDF, стилизованные под фирменный шаблон (шапка АНО ВО, Times New Roman,
  Univers LT CYR), шрифты и логотип берутся из `../fonts` и `../pics`.
""")
